# HMM-Diffusion dependencies, loose bounds so the same file works on a CUDA box and on Apple Silicon.
# requirements-ruya-freeze.txt holds the exact CUDA 12.4 versions the diffusion code was tuned on;
# fall back to it if the resolution below misbehaves.

# Core
numpy>=1.24,<3
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pyarrow>=14.0
tqdm>=4.65
PyYAML>=6.0

# Diffusion stack.
# On CUDA install torch from the appropriate index first, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.4
einops>=0.7
# Imported at trainer startup (losses.py / plotting.py), even when those terms are unused.
geomloss>=0.2.6
seaborn>=0.13
tslearn>=0.6

# Regime labeling (Vol_Regime): ARCH conditional volatility and PELT changepoints.
arch>=6.2
ruptures>=1.1

# HMM fitting. NumPyro/JAX drives the three MCMC variants, Pyro drives the neural HMM.
numpyro>=0.13
jax>=0.4.20
jaxlib>=0.4.20
funsor>=0.4.5
pyro-ppl>=1.8.6

# Downstream volatility forecasting (Downstream-Volatility.ipynb).
ta>=0.11

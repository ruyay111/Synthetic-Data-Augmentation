# Pinned to the environment used for the EW pipeline (conda env `diff`).
# On CUDA, install torch from the matching index first, for example:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu124

# Core
numpy==2.0.2
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2
matplotlib==3.9.2
pyarrow==18.0.0
tqdm==4.67.0
PyYAML==6.0.2

# Diffusion stack
torch==2.5.1
einops==0.8.0
geomloss==0.2.6
seaborn==0.13.2
tslearn==0.6.3

# Regime labeling (Vol_Regime): ARCH conditional volatility and PELT changepoints
arch==8.0.0
ruptures==1.1.10

# HMM fitting. NumPyro/JAX drives the MCMC variants; Pyro drives the neural HMM
numpyro==0.21.0
jax==0.10.2
jaxlib==0.10.2
funsor==0.4.8
pyro-ppl==1.9.1

# Downstream volatility / return forests
ta==0.11.0
